# Block Header Hash

The task is to derive a block header hash using a given block header data

### Install Dependencies
Run `npm install`

### Run start script
Run `npm run start` to derive block header function
