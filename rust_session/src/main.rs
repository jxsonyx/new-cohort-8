mod sub;
mod sum;
mod ownership;
mod array;


fn main() {
    // use sub::sub;
    // sub(10, 5);
    // ownership::test_ownership();
    // ownership::call_name();
    // array::test_array();
    ownership::test_move();
}
